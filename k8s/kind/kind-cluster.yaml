kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 3001
        hostPort: 3001 # For Grafana access (adjust ports as needed)
        protocol: TCP
      - containerPort: 8080
        hostPort: 8080 # For Node.js app
        protocol: TCP
  - role: worker
  - role: worker
